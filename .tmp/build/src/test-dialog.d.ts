import DialogConstructorOptions = powerbi.extensibility.visual.DialogConstructorOptions;
export declare class TestDialog {
    static id: string;
    private target;
    private host;
    constructor(options: DialogConstructorOptions, initialState: object);
}
